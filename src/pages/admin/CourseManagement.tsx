import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const CourseManagement: React.FC = () => {
  return (
    <div>
      <Title level={2}>課程管理</Title>
      <Card>
        <p>課程管理頁面 - 開發中</p>
      </Card>
    </div>
  )
}

export default CourseManagement 