import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const AdminDashboard: React.FC = () => {
  return (
    <div>
      <Title level={2}>管理員儀表板</Title>
      <Card>
        <p>管理員儀表板頁面 - 開發中</p>
      </Card>
    </div>
  )
}

export default AdminDashboard 