import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const TeacherDashboard: React.FC = () => {
  return (
    <div>
      <Title level={2}>老師儀表板</Title>
      <Card>
        <p>老師儀表板頁面 - 開發中</p>
      </Card>
    </div>
  )
}

export default TeacherDashboard 