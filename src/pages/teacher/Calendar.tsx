import React from 'react'
import { Card, Typography } from 'antd'

const { Title } = Typography

const TeacherCalendar: React.FC = () => {
  return (
    <div>
      <Title level={2}>老師課表</Title>
      <Card>
        <p>老師課表頁面 - 開發中</p>
      </Card>
    </div>
  )
}

export default TeacherCalendar 